<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="MotionToolbarButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="MinWidth" Value="88" />
        <Setter Property="Height" Value="34" />
        <Setter Property="Margin" Value="4,0" />
        <Setter Property="Padding" Value="12,0" />
        <Setter Property="Foreground" Value="{StaticResource MotionBrush.TextPrimary}" />
        <Setter Property="Background" Value="{StaticResource MotionBrush.PanelBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource MotionBrush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource MotionBrush.ButtonHover}" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource MotionBrush.Accent}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MotionRunButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource MotionToolbarButtonStyle}">
        <Setter Property="Background" Value="{StaticResource MotionBrush.Success}" />
        <Setter Property="BorderBrush" Value="{StaticResource MotionBrush.Success}" />
        <Setter Property="Foreground" Value="{StaticResource MotionBrush.LightText}" />
    </Style>

    <Style x:Key="MotionStopButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource MotionToolbarButtonStyle}">
        <Setter Property="Background" Value="{StaticResource MotionBrush.Warning}" />
        <Setter Property="BorderBrush" Value="{StaticResource MotionBrush.Warning}" />
        <Setter Property="Foreground" Value="{StaticResource MotionBrush.LightText}" />
    </Style>

    <Style x:Key="MotionEmergencyButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource MotionToolbarButtonStyle}">
        <Setter Property="MinWidth" Value="118" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Background" Value="{StaticResource MotionBrush.Danger}" />
        <Setter Property="BorderBrush" Value="{StaticResource MotionBrush.Danger}" />
        <Setter Property="Foreground" Value="{StaticResource MotionBrush.LightText}" />
    </Style>
</ResourceDictionary>
